<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>

  <!-- ========================================================= -->
  <!-- 1. Apple system font aliases                              -->
  <!-- ========================================================= -->

  <match target="pattern">
    <test name="family" compare="eq">
      <string>-apple-system</string>
    </test>
    <edit name="family" mode="assign_replace">
      <string>SF Pro Text</string>
    </edit>
  </match>

  <match target="pattern">
    <test name="family" compare="eq">
      <string>BlinkMacSystemFont</string>
    </test>
    <edit name="family" mode="assign_replace">
      <string>SF Pro Text</string>
    </edit>
  </match>

  <!-- ========================================================= -->
  <!-- 2. Size-based Text → Display switching (macOS-like)       -->
  <!-- ========================================================= -->

  <!-- Use SF Pro Display for large text -->
  <match target="pattern">
    <test name="family" compare="eq">
      <string>SF Pro Text</string>
    </test>
    <test name="size" compare="more">
      <double>19</double>
    </test>
    <edit name="family" mode="assign_replace">
      <string>SF Pro Display</string>
    </edit>
  </match>

  <!-- ========================================================= -->
  <!-- 3. Generic family mappings                                -->
  <!-- ========================================================= -->

  <!-- sans-serif → SF Pro Text -->
  <match target="pattern">
    <test name="family" compare="eq">
      <string>sans-serif</string>
    </test>
    <edit name="family" mode="assign_replace">
      <string>SF Pro Text</string>
    </edit>
  </match>

  <!-- serif → Times (macOS default) -->
  <match target="pattern">
    <test name="family" compare="eq">
      <string>serif</string>
    </test>
    <edit name="family" mode="assign_replace">
      <string>Times New Roman</string>
    </edit>
  </match>

  <!-- monospace → SF Mono -->
  <match target="pattern">
    <test name="family" compare="eq">
      <string>monospace</string>
    </test>
    <edit name="family" mode="assign_replace">
      <string>SF Mono</string>
    </edit>
  </match>

  <!-- ========================================================= -->
  <!-- 4. Web-safe aliases (match macOS behavior)                -->
  <!-- ========================================================= -->

  <match target="pattern">
    <test name="family" compare="eq">
      <string>Helvetica</string>
    </test>
    <edit name="family" mode="assign_replace">
      <string>SF Pro Text</string>
    </edit>
  </match>

  <match target="pattern">
    <test name="family" compare="eq">
      <string>Arial</string>
    </test>
    <edit name="family" mode="assign_replace">
      <string>SF Pro Text</string>
    </edit>
  </match>

  <!-- ========================================================= -->
  <!-- 5. Emoji isolation (CRITICAL)                              -->
  <!-- ========================================================= -->

  <!-- Never allow emoji fonts to hijack text -->
  <alias>
    <family>sans-serif</family>
    <prefer>
      <family>SF Pro Text</family>
    </prefer>
  </alias>

  <alias>
    <family>monospace</family>
    <prefer>
      <family>SF Mono</family>
    </prefer>
  </alias>

  <!-- Emoji only when explicitly requested -->
  <match target="pattern">
    <test name="family" compare="eq">
      <string>emoji</string>
    </test>
    <edit name="family" mode="assign_replace">
      <string>Apple Color Emoji</string>
    </edit>
  </match>

</fontconfig>
